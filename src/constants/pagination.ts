/**
 * Default page size options for pagination across the application
 */
export const DEFAULT_PAGE_SIZE_OPTIONS = [10, 25, 50, 100];

/**
 * Default page size
 */
export const DEFAULT_PAGE_SIZE = 10;
